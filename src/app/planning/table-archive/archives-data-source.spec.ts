import { ArchivesDataSource } from './archives-data-source';

describe('ArchivesDataSource', () => {
  it('should create an instance', () => {
    expect(new ArchivesDataSource()).toBeTruthy();
  });
});
